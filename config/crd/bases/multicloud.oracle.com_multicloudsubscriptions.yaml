---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: multicloudsubscriptions.multicloud.oracle.com
spec:
  group: multicloud.oracle.com
  names:
    kind: MulticloudSubscription
    listKind: MulticloudSubscriptionList
    plural: multicloudsubscriptions
    singular: multicloudsubscription
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: status of the MulticloudSubscription
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the MulticloudSubscription
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: MulticloudSubscription is the Schema for the multicloudsubscriptions
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MulticloudSubscriptionSpec defines the desired state of MulticloudSubscription.
            type: object
          status:
            description: MulticloudSubscriptionStatus defines the observed state of
              MulticloudSubscription.
            properties:
              activeCommitment:
                description: Total value for the subscription.
                type: string
              classicSubscriptionId:
                description: Subscription ID for OCI and Partner cloud in classic
                  format.
                type: string
              cspAdditionalProperties:
                additionalProperties:
                  type: string
                description: CSP Specific Additional Properties, AzureSubnetId for
                  Azure
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              lifecycleState:
                description: The current state of the subscription.
                type: string
              partnerCloudAccountIdentifier:
                description: The partner cloud account ID.
                type: string
              paymentPlan:
                description: Payment plan for the subscription.
                type: string
              serviceName:
                description: The serviceName that externalLocation map object belongs
                  to.
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              subscriptionId:
                description: URL to the subscription page https://{console-url}/org-mgmt/subscription/ocid1.organizationssubscription.oc1.iad.amaaaaaapf266qyaqohz27zvh45jzaielgwojo53bh24s7cy5q5g7fiknpxa?region=us-ashburn-1.
                type: string
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the subscription was created, in the format defined by
                  RFC 3339 (https://tools.ietf.org/rfc/rfc3339).
                type: string
              timeEndDate:
                description: |-
                  The date and time for when the subscription is finishing, in the format defined by
                  RFC 3339 (https://tools.ietf.org/rfc/rfc3339).
                type: string
              timeLinkedDate:
                description: |-
                  The date and time for when the multicloud was created, in the format defined by
                  RFC 3339 (https://tools.ietf.org/rfc/rfc3339).
                type: string
              timeUpdated:
                description: |-
                  The date and time the subscription was updated, in the format defined by
                  RFC 3339 (https://tools.ietf.org/rfc/rfc3339).
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
